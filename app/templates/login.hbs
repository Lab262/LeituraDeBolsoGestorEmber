<div class="login-form">
  <div class="container">

    {{#if authManager.currentUser}}
    Logged in as {{authManager.currentUser.email}}
    {{ else }}


    {{login-form}}


    {{/if}}

  </div>
</div>
